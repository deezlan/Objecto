# Always normalize text files to LF
* text=auto eol=lf

# Binary files (images, models)
*.png binary
*.jpg binary
*.glb binary
*.fbx binary
*.unity binary
*.prefab binary

# Lighting files
*.exr filter=lfs diff=lfs merge=lfs -text
*.hdr filter=lfs diff=lfs merge=lfs -text